using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using smartData.Controllers;
using ServiceLayer.Services;
using smartData.Models;
using System.Collections.Generic;
using Core.Domain;
namespace UnitTests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //Arrange
            //LoginViewModel userModel = new LoginViewModel();
            //userModel.UserName = "samirso@smartdatainc.net";
            //userModel.Password = "welcome";
            //userModel.RememberMe = false;
            //ServiceLayer.Services.ResetPasswordService _ResetPasswordService = new ServiceLayer.Services.ResetPasswordService();
            //List<User> list = _ResetPasswordService.GetUsersByEmail(userModel.UserName.ToString());

            //Assert.IsTrue(list.Count > 0);
        }
    }
}
